<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.liuwangshu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Retrofit.GetRequestActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Retrofit.TestRetrofitActivity"
            android:exported="true">

        </activity>
        <activity android:name=".内存泄露.TestActivity"></activity>
        <activity android:name=".test.activity.ObservableTestActivity" />
        <activity android:name=".light.activity.TestCustomViewActivity" />
        <activity android:name=".light.activity.TextInputLayoutActivity" />
        <activity android:name=".light.activity.SnackbarActivity" />
        <activity android:name=".other.activity.SecondActivity" />
        <activity android:name=".other.activity.EventTestActivity" />
        <activity android:name=".MainActivity" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>